exec = hyprpm reload -n

# Device-specific configuration
source = local.conf

# keybinds
source = keybinds.conf

# Variables
source = vars.conf

# Autostart
source = autostart.conf

# Theming
source = theming.conf

# Miscellanious Options
source = options.conf

# Layout options
source = layout.conf

# Plugin-specific configs
source = plugins.conf

# Window-, workspace- and layerrules
source = windowrules.conf
