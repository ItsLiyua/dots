set default-bg                  "{{ background }}" 
set default-fg                  "{{ foreground }}"

set statusbar-bg                "{{ background }}"
set statusbar-fg                "{{ foreground }}"

set inputbar-bg                 "{{ background }}" 
set inputbar-fg                 "{{ foreground }}" 

set notification-error-bg       "{{ color1 }}"
set notification-error-fg       "{{ color0 }}"
set notification-warning-bg     "{{ background }}"
set notification-warning-fg     "{{ color1 }}"
set notification-bg             "{{ background }}"
set notification-fg             "{{ color4 }}"

set highlight-color             "rgba({{ color3 | rgb }}, 0.5)"
set highlight-active-color      "rgba({{ color3 | complementary | rgb }}, 0.5)"

set recolor-lightcolor          "{{ background }}"
set recolor-darkcolor           "{{ foreground }}"
set recolor "true"

